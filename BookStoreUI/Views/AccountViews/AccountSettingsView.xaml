<UserControl x:Class="BookStoreUI.Views.AccountViews.AccountSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookStoreUI.Views.AccountViews" xmlns:converters="clr-namespace:BookStoreUI.Converters">
    <UserControl.Resources>
        <converters:BooleanToAccountModeConverter x:Key="BooleanToAccountModeConverter"></converters:BooleanToAccountModeConverter>
        <Style TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Disabled"></Setter>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"></Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Stylus.IsFlicksEnabled" Value="True" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="6"/>
                    <Setter Property="MinHeight" Value="6"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Width="600" Height="450" VerticalAlignment="Center" HorizontalAlignment="Center">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="30"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Grid.Column="0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="120" Height="50" Command="{Binding GoToPreviousViewCommand}">
            <StackPanel Orientation="Horizontal">
                <Polygon Fill="White" Points="20,5 20,30 0,18"></Polygon>
                <TextBlock Margin="15,0,0,0" FontSize="24" Foreground="White">Back</TextBlock>
            </StackPanel>
        </Button>

        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" FontSize="36" Text="{Binding IsAdmin, Converter={StaticResource BooleanToAccountModeConverter}, StringFormat='Account settings ({0})'}"></TextBlock>

        <ScrollViewer Grid.Row="4" Grid.Column="0">
            <TextBlock FontSize="26" TextAlignment="Left" Text="{Binding Name, StringFormat='Name: {0}'}"></TextBlock>
        </ScrollViewer>

        <ScrollViewer Grid.Row="5" Grid.Column="0">
            <TextBlock FontSize="26" TextAlignment="Left" Text="{Binding MiddleName, StringFormat='Middle name: {0}'}"></TextBlock>
        </ScrollViewer>

        <ScrollViewer Grid.Row="6" Grid.Column="0">
            <TextBlock FontSize="26" TextAlignment="Left" Text="{Binding LastName, StringFormat='Last name: {0}'}"></TextBlock>
        </ScrollViewer>

        <ScrollViewer Grid.Row="4" Grid.Column="2">
            <TextBlock FontSize="26" TextAlignment="Left" Margin="10,0,0,0" Text="{Binding Login, StringFormat='Login: {0}'}"></TextBlock>
        </ScrollViewer>

        <Grid Grid.Row="5" Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding NewLogin, UpdateSourceTrigger=PropertyChanged}" Margin="10,25"></TextBox>

            <Button Grid.Column="1" Command="{Binding ChangeLoginCommand}" Margin="10,20" FontSize="18">Change</Button>
        </Grid>

        <Button Grid.Row="6" Grid.Column="2" Content="Change password" Command="{Binding ChangePasswordCommand}"></Button>
        
    </Grid>
</UserControl>