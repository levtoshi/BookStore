<UserControl x:Class="BookStoreUI.Views.OtherViews.AddDelayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookStoreUI.Views.OtherViews">
    <Grid Height="425" Width="700" HorizontalAlignment="Center" VerticalAlignment="Center">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="120" Height="50" Command="{Binding GoToPreviousViewCommand}">
            <StackPanel Orientation="Horizontal">
                <Polygon Fill="White" Points="20,5 20,30 0,18"></Polygon>
                <TextBlock Margin="15,0,0,0" FontSize="24" Foreground="White" FontWeight="SemiBold">Back</TextBlock>
            </StackPanel>
        </Button>

        <Grid Grid.Row="1" Margin="0,25,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="1.6*"></RowDefinition>
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0">Customer name:</TextBlock>
            <TextBlock Grid.Column="1" Grid.Row="0">Middle name:</TextBlock>
            <TextBlock Grid.Column="2" Grid.Row="0">Last name:</TextBlock>

            <TextBox Grid.Column="0" Grid.Row="1" Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding CustomerMiddleName, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <TextBox Grid.Column="2" Grid.Row="1" Text="{Binding CustomerLastName, UpdateSourceTrigger=PropertyChanged}"></TextBox>

            <Grid Width="500" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="2" Grid.RowSpan="2">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0">Email:</TextBlock>
                <TextBlock Grid.Column="1" Grid.Row="0">Amount of books:</TextBlock>

                <TextBox Grid.Column="0" Grid.Row="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            </Grid>

            <Button Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="4" Margin="100,15" Command="{Binding SubmitCommand}">Add delay</Button>
        </Grid>
    </Grid>
</UserControl>